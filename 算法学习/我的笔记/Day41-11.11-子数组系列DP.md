# Day41 - 子数组系列DP

**日期**：2025年11月11日  
**学习重点**：子数组系列DP继续练习

---

## 📋 今日计划

### 子数组系列DP
- [x] 最长连续递增序列（经典子数组DP）⭐ ✅ 

---

## 🎯 学习目标

1. 继续巩固子数组DP的状态设计
2. 练习不同类型的子数组问题

---

## 📝 题目记录

### 题目1：最长连续递增序列 ⭐

**题目链接**：[LeetCode 674 - 最长连续递增序列](https://leetcode.cn/problems/longest-continuous-increasing-subsequence/)

**核心思路**：
- 典型子数组DP，定义`dp[i]`表示以`nums[i]`结尾的最长连续递增序列长度
- 如果`nums[i] > nums[i-1]`，可以延续前面的序列：`dp[i] = dp[i-1] + 1`
- 否则重新开始：`dp[i] = 1`
- 答案需要遍历所有`dp[i]`取最大值（**注意：不是返回`dp[n-1]`！**）

**AC代码**：

```cpp
class Solution {
public:
    int findLengthOfLCIS(vector<int>& nums) {
        int n = nums.size();
        vector<int> dp(n);
        
        dp[0] = 1;
        int ret = dp[0];  // 初始化为dp[0]
        
        for(int i = 1; i < n; i++) {
            if(nums[i] > nums[i-1]) {
                dp[i] = dp[i-1] + 1;
            } else {
                dp[i] = 1;
            }
            ret = max(ret, dp[i]);  // 在循环中更新最大值
        }
        
        return ret;  // 返回最大值，不是dp[n-1]！
    }
};
```

**空间优化版本**：

```cpp
class Solution {
public:
    int findLengthOfLCIS(vector<int>& nums) {
        int n = nums.size();
        int len = 1, ret = 1;  // len: 当前连续递增序列长度
        
        for(int i = 1; i < n; i++) {
            if(nums[i] > nums[i-1]) {
                len++;
            } else {
                len = 1;  // 重新开始
            }
            ret = max(ret, len);
        }
        
        return ret;
    }
};
```

**关键点**：
- 子数组DP的经典模板：以`i`结尾 + 遍历所有结尾取最大值
- 注意答案不一定是`dp[n-1]`，需要取所有状态的最大值
- 可以空间优化到O(1)，只需记录当前长度

---

## 💡 今日总结

### 核心收获

### 关键技巧

---

*笔记格式：简洁记录核心思路和AC代码*  
*Cursor Write It-qcf*

